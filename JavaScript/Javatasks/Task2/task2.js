let count = 0;
let max = 10;


for (let count = 0; count < max; count++) {
    
    if (count == 5) 
    {
        console.log(`Woah Lochi i counted to ${count}!`);
    }
    else if (count == 9) {
        console.log(`look Lochi i can count to ${count}!`);
    }
    else {
        console.log(`Counting up... I'm at: ${count}`);
    }
    
}